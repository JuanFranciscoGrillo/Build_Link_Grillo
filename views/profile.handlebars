{{#extend "layouts/main"}}
    {{#content "body"}}
        <h1>User Profile</h1>
        <div class="profile-info">
            <!-- User information -->
        </div>
        <div class="user-listings">
            <h2>My Job Listings</h2>
            <!-- Listing of user's job postings -->
        </div>
    {{/content}}
{{/extend}}
